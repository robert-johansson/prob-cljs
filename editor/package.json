{
  "private": true,
  "scripts": {
    "build:editor": "esbuild src/editor.js --bundle --format=iife --global-name=ProbEditor --outfile=../docs/js/editor.js --minify",
    "build:physics": "esbuild src/physics.js --bundle --format=iife --global-name=ProbPhysics --outfile=../docs/js/physics.js --minify",
    "build": "npm run build:editor && npm run build:physics",
    "watch": "esbuild src/editor.js --bundle --format=iife --global-name=ProbEditor --outfile=../docs/js/editor.js --watch"
  },
  "dependencies": {
    "@nextjournal/clojure-mode": "^0.3.3",
    "@codemirror/view": "^6.0.0",
    "@codemirror/state": "^6.0.0",
    "@codemirror/commands": "^6.0.0",
    "@codemirror/language": "^6.0.0",
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
